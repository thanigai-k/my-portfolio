---
import { Image } from "astro:assets";

interface Props {
    src: ImageMetadata;
    alt?: string;
}
const { src, alt = "" } = Astro.props;
---

<Image
    src={src}
    alt={alt}
    class="rounded-card aspect-video object-cover object-center"
/>
